# Caddy reverse proxy for VPN4Friends Mini App API
# Domain is taken from API_PUBLIC_DOMAIN env variable passed via docker-compose.

{$API_PUBLIC_DOMAIN} {
    encode gzip
    reverse_proxy http://host.docker.internal:8000
}
